input {
  # TCP input for Spring Boot application logs (JHipster)
  tcp {
    port => 5044
    codec => json_lines
  }
  
  # File input for application logs (if mounted)
  file {
    path => "/var/log/app/*.log"
    start_position => "beginning"
    codec => json_lines
    sincedb_path => "/dev/null"
  }
}

filter {
  # JHipster sends logs as JSON, so we can use them directly
  # Add @timestamp if not present
  if ![timestamp] {
    date {
      match => [ "@timestamp", "ISO8601" ]
    }
  } else {
    date {
      match => [ "timestamp", "ISO8601" ]
      target => "@timestamp"
    }
  }
  
  # Ensure we have a message field
  if [message] {
    # Message is already parsed from JSON
  } else if [log] {
    mutate {
      add_field => { "message" => "%{[log][message]}" }
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
  
  # Also output to stdout for debugging
  stdout {
    codec => rubydebug
  }
}



